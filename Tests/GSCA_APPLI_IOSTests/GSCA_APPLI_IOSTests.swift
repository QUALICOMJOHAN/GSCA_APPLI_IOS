import XCTest
@testable import GSCA_APPLI_IOS

final class GSCA_APPLI_IOSTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GSCA_APPLI_IOS().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
